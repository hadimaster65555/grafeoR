% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/openflights.R
\name{openflights_sample_data}
\alias{openflights_sample_data}
\title{Load the bundled OpenFlights sample dataset}
\usage{
openflights_sample_data()
}
\value{
A named list with \code{airports}, \code{routes}, and \code{metadata}.
}
\description{
\code{openflights_sample_data()} returns a compact real-world subset of the
OpenFlights airport and route data bundled with \code{grafeoR}. The sample keeps
the 20 airports with the highest outbound route counts in the upstream
snapshot and the airline route records whose source and destination are both
in that subset.
}
\details{
The data is intended for examples, demos, and smoke tests that exercise the
Grafeo bindings with non-trivial graph data while remaining fully offline.
}
\examples{
sample <- openflights_sample_data()
dim(sample$airports)
dim(sample$routes)
}
