# Convert a Grafeo result to a data frame

Convert a Grafeo result to a data frame

## Usage

``` r
# S3 method for class 'grafeo_result'
as.data.frame(x, ...)
```

## Arguments

- x:

  A `grafeo_result`.

- ...:

  Unused.

## Value

A base `data.frame`.
